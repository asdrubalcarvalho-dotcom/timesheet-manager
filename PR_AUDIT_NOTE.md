This PR exists only to provide a review/CI trail.

The actual changes were already merged to main via a bypassed push:
- Env-driven SuperAdmin domain/origin allowlists
- Tenant daily metrics "today" boundaries use tenant timezone
- .env.example updated with new vars
